// Utility Funs
const $ = (selector) => document.querySelector(selector);

// Actual code

// Selectors
const $todoList = $('#todo-list');

const $form = $('#form');

const $input = $('#input');

const $btnSubmit = $('#btn-submit');

export { $todoList, $form, $input, $btnSubmit };
